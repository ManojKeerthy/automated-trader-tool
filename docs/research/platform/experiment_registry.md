# EXPERIMENT REGISTRY SPECIFICATION

The `ExperimentRegistry` records full environment reproducibility metadata alongside experiment versions:
- Python version, OS, package versions (`pip freeze`), CPU, RAM, execution time, execution hash, Git commit, and random seed.
