# PLAYBOOK: FAILURE ANALYSIS

When an experiment fails its Development Survivor Gate:
1. Do NOT lower gate thresholds.
2. Log failure code in `ResearchGraveyard`.
3. Document failure mechanism (`FRICTION_EROSION`, `LOW_WIN_RATE`, `SIGNAL_SCARCITY`, etc.).
4. Lock strategy lineage against future parameter tweaks.
